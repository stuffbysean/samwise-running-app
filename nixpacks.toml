[phases.setup]
nixPkgs = ['nodejs_18', 'npm']

[phases.install]
cmds = ['npm install --prefix backend']

[phases.build]
cmds = []

[start]
cmd = 'cd backend && npm start'

[variables]
NODE_ENV = 'production'
PORT = '3000'